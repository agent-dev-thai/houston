interface:
  display_name: "Houston"
  short_description: "Run mission-mode coding with fanout."
  default_prompt: "Use $houston to create or resume a mission, delegate safe lanes with subagents when useful, validate, and write the handoff."

policy:
  allow_implicit_invocation: true
